[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:jose.*
    ignore::DeprecationWarning:pydantic.*
markers =
    integration: tests que requieren la app y la DB
