# Dependencias solo para tests / dev (no requeridas en runtime de produccion).
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
freezegun>=1.5
ruff>=0.5

# Driver async SQLite usado por los tests (engine in-memory). En produccion
# el driver es asyncpg/Postgres; aiosqlite solo se necesita en CI/local.
aiosqlite>=0.20
